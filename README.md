# ESP32SoloBCHAssess
YES — BCH solo mining on ESP32 CYD/WROOM is possible, but with hard constraints. BCH uses SHA-256d (same as BTC), so existing firmware works — the bottleneck is finding a solo pool that accepts low-difficulty connections from ESP32-class hardware. 
